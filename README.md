# Data Generator

Just a basic data generator - still debating if it will be a CLI tool, web tool, or both. The aim is simply to create a tool that can generate mock data (tables to start then JSON later) given some parameters:

- Table/JSON
- Fields (Name, Address, Phone #, Temps)
- Type (int, string, bool etc)
